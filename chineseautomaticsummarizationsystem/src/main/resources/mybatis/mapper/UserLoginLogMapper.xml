<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ncu.dao.mapper.UserLoginLogMapper">

    <resultMap id="UserLoginLogEntity" type="cn.edu.ncu.dao.entity.UserLoginLog"></resultMap>
    <sql id="queryByPageWhere">
        <trim prefix="where" prefixOverrides="and|or">
            <if test="queryDTO.startDate != null and queryDTO.startDate != ''">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &gt;= #{queryDTO.startDate}
            </if>
            <if test="queryDTO.endDate != null and queryDTO.endDate != ''">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &lt;= #{queryDTO.endDate}
            </if>
            <if test="queryDTO.userName != null and queryDTO.userName != ''">
                AND INSTR(user_name,#{queryDTO.userName})
            </if>
        </trim>
    </sql>
    <sql id="orderByPage">
        <choose>
            <when test="queryDTO.orders !=null and queryDTO.orders.size()>0">
                order by
                <foreach collection="idList"  separator="," item="item">
                    ${item.column}
                    <choose>
                        <when test="item.asc">
                            asc
                        </when>
                        <otherwise>
                            desc
                        </otherwise>
                    </choose>
                </foreach>
                ,create_time desc
            </when>
            <otherwise>
                order by create_time desc
            </otherwise>
        </choose>

        LIMIT ${(queryDTO.pageNum - 1) * queryDTO.pageSize},#{queryDTO.pageSize}
    </sql>
    <select id="queryCount" resultType="integer">
        select count(*) from user_login_log
        <include refid="queryByPageWhere"/>
    </select>
    <select id="queryByPage" resultMap="UserLoginLogEntity">
        select
          id,
          user_id,
          user_name,
          remote_ip,
          remote_port,
          remote_browser,
          remote_os,
          login_status,
          update_time,
          create_time
        from user_login_log
        <include refid="queryByPageWhere"/>
        <include refid="orderByPage"/>
    </select>

    <delete id="deleteById">
        delete from user_login_log where id = #{id}
    </delete>

    <delete id="deleteByIds">
        delete from user_login_log where id in
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </delete>

</mapper>