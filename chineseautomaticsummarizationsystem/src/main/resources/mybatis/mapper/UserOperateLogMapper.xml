<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ncu.dao.mapper.UserOperateLogMapper">

    <resultMap id="UserOperateLogEntity" type="cn.edu.ncu.dao.entity.UserOperateLog"></resultMap>

    <sql id="queryByPageWhere">
        <trim prefix="where" prefixOverrides="and|or">
            <if test="queryDTO.startDate != null and queryDTO.startDate != ''">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &gt;= #{queryDTO.startDate}
            </if>
            <if test="queryDTO.endDate != null and queryDTO.endDate != ''">
                AND DATE_FORMAT(create_time, '%Y-%m-%d') &lt;= #{queryDTO.endDate}
            </if>
            <if test="queryDTO.userName != null and queryDTO.userName != ''">
                AND INSTR(user_name,#{queryDTO.userName})
            </if>
            <if test="queryDTO.resultFlag != null">
                AND result = #{queryDTO.resultFlag}
            </if>
        </trim>
    </sql>
    <sql id="orderByPage">
        <choose>
            <when test="queryDTO.orders !=null and queryDTO.orders.size()>0">
                order by
                <foreach collection="idList"  separator="," item="item">
                    ${item.column}
                    <choose>
                        <when test="item.asc">
                            asc
                        </when>
                        <otherwise>
                            desc
                        </otherwise>
                    </choose>
                </foreach>
                ,create_time desc
            </when>
            <otherwise>
                order by create_time desc
            </otherwise>
        </choose>

        LIMIT ${(queryDTO.pageNum - 1) * queryDTO.pageSize},#{queryDTO.pageSize}
    </sql>
    <update id="update">
        update user_operate_log
        <set>
            <if test="userName !=null and userName!=''">
                user_name=#{userName},
            </if>
            <if test="userId !=null ">
                user_id=#{userId},
            </if>
            <if test="module !=null and module!=''">
                module=#{module},
            </if>
            <if test="content !=null and content!=''">
                content=#{content},
            </if>
            <if test="url !=null and url!=''">
                url=#{url},
            </if>
            <if test="method !=null and method!=''">
                method=#{method},
            </if>
            <if test="param !=null and param!=''">
                param=#{param},
            </if>
            <if test="result !=null ">
                result=#{result},
            </if>
            <if test="failReason !=null and failReason!=''">
                fail_reason=#{failReason},
            </if>
            <if test="updateTime !=null ">
                update_time=#{updateTime},
            </if>
        </set>
        where id=#{id}
    </update>
    <select id="queryCount" resultType="integer">
        select count(*) from user_operate_log
        <include refid="queryByPageWhere"/>
    </select>
    <select id="queryByPage" resultMap="UserOperateLogEntity">
        select
          id,
          user_id,
          user_name,
          module,
          content,
          url,
          method,
          param,
          result,
          fail_reason,
          update_time,
          create_time
        from user_operate_log
        <include refid="queryByPageWhere"/>
        <include refid="orderByPage"/>
    </select>

    <delete id="deleteById">
        delete from user_operate_log where id = #{id}
    </delete>

    <delete id="deleteByIds">
        delete from user_operate_log where id in
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </delete>

</mapper>