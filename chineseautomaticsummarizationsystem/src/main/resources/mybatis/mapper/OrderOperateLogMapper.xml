<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ncu.dao.mapper.OrderOperateLogMapper">
    <resultMap id="orderOperateLogSaveDTOMap" type="cn.edu.ncu.dao.entity.OrderOperateLog"></resultMap>


    <select id="listOrderOperateLogsByOrderTypeAndOrderId" resultMap="orderOperateLogSaveDTOMap">
        select * from order_operate_log where order_id= #{orderId}
        AND order_type IN
        <foreach collection="orderTypeList" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
        order by id desc
    </select>

    <select id="listOrderOperateLogsByOrderTypeAndOrderIds" resultMap="orderOperateLogSaveDTOMap">
        select * from order_operate_log where
        order_id IN
        <foreach collection="orderIds" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
        AND order_type IN
        <foreach collection="orderTypeList" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
        order by id desc
    </select>



    <insert id="batchInsert"  parameterType="java.util.List">
        INSERT INTO order_operate_log (id,order_id,order_type, operate_type, operate_content, operate_remark, employee_id, employee_name,ext_data,update_time,create_time) VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id},
            #{item.orderId},
            #{item.orderType},
            #{item.operateType},
            #{item.operateContent},
            #{item.operateRemark},
            #{item.employeeId},
            #{item.employeeName},
            #{item.extData},
            #{item.updateTime}
            #{item.createTime}
            )
        </foreach>
    </insert>


</mapper>
