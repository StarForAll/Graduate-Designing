<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.ncu.dao.mapper.DepartmentMapper">


    <resultMap id="DepartmentVO" type="cn.edu.ncu.pojo.vo.DepartmentVO"></resultMap>


    <sql id="baseSql">
        d.id,
        d.name,
        d.short_name,
        d.manager_id,
        d.parent_id,
        d.sort,
        d.update_time,
        d.create_time
    </sql>

    <select id="listAll" resultMap="DepartmentVO">
        SELECT
        <include refid="baseSql"></include>,
        e.actual_name as managerName,
        pd.`name` as parentName
        FROM
        department d
        left join employee e on d.manager_id= e.id
        left join department pd on d.parent_id = pd.id
        order by sort asc
    </select>

    <select id="selectByParentId" resultMap="DepartmentVO">
        SELECT
        <include refid="baseSql"></include>
        FROM
        department d
        where
        d.parent_id = #{departmentId}
        order by sort desc
    </select>


    <select id="countSubDepartment" resultType="java.lang.Integer">
        SELECT
        count(1)
        FROM
        department
        WHERE
        parent_id = #{deptId}
    </select>
    <update id="update">
        update department
        <set>
            <if test="name!=null and name!=''">
                name=#{name},
            </if>
            <if test="shortName!=null and shortName!=''">
                short_name=#{shortName},
            </if>
            <if test="managerId!=null ">
                manager_id=#{managerId},
            </if>
            <if test="parentId!=null ">
                parent_id=#{parentId},
            </if>
            <if test="sort!=null ">
                sort=#{sort},
            </if>
            <if test="updateTime!=null ">
                update_time=#{updateTime},
            </if>
        </set>
        where id=#{id}
    </update>

</mapper>